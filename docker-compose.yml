services:
  erebor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: erebor
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      - "127.0.0.1:3000:3000"
